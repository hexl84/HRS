@model IEnumerable<StaffSystemDemo.Models.Staff>

@{
    ViewBag.Title = "Index";
}
<script src="/Scripts/jquery-1.8.2.min.js" type="text/javascript"></script>
<script src="/Scripts/jquery-1.8.2.js" type="text/javascript"></script>
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Add")
    &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
   
    @using (Html.BeginForm("Search", "Staff"))
    {
    <p>
        @*Genre: @Html.DropDownList("movieGenre", "All")*@
          &nbsp;  Title: @Html.TextBox("name")  
        <input type="submit" value="Filter" />
    </p>
    }
</p>
<table id="table1">
    <tr id="tr_0">
        <th>
            JobNumber
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BirthDay)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.School)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WorkExperience)
        </th>
        
        <th></th>
    </tr>

    @foreach (var item in Model)
{
    <tr id="tr_@item.Id">
        <td>
            @Html.DisplayFor(modelItem => item.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BirthDay)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.School)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WorkExperience)
        </td>


        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Lock", "Lock", new { id = item.Id })
        </td>
    </tr>
}

</table>

<script type="text/javascript">
   
</script>
照片 附件